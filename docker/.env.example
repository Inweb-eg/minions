# Minions Docker Environment Configuration
# ==========================================
# Copy this file to .env and customize as needed

# Node environment
NODE_ENV=production

# Minions web interface port
MINIONS_PORT=2505

# Ollama settings
OLLAMA_PORT=11434
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.2:3b

# Gemini API key (fallback when Ollama is unavailable)
# Get your key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Path to your projects directory (for existing project support)
# The container will mount this as read-only
PROJECTS_PATH=/path/to/your/projects
